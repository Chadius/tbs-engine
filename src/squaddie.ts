export class Squaddie {
  maxHealth: number

  constructor(maxHealth) {
    this.maxHealth = maxHealth
  }

  getMaxHealth() {
    return this.maxHealth
  }

  getHealth() {
    return this.maxHealth
  }
}